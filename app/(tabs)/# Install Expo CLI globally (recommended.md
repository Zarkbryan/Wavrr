# Install Expo CLI globally (recommended for beginners)
npm install -g expo-cli

# Create a new React Native project
expo init MyReactNativeApp

# Navigate into your project directory
cd MyReactNativeApp

# Start the development server
expo start